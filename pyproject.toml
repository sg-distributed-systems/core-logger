[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "core-logger"
version = "0.2.0"
description = "Shared structured logging library for microservices"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
